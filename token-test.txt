ADMIN_TOKEN=$(curl -s -X POST "http://localhost:8080/realms/NieTylkoQuizy/protocol/openid-connect/token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=password" \
  -d "client_id=NieTylkoQuizyClient" \
  -d "username=admin" \
  -d "password=admin1" | jq -r '.access_token')

  curl -s -X GET "http://localhost:5002/api/users/"   -H "Authorization: Bearer $ADMIN_TOKEN" -H "Content-Type: application/json"

USER1_TOKEN=$(curl -s -X POST "http://localhost:8080/realms/NieTylkoQuizy/protocol/openid-connect/token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=password" \
  -d "client_id=NieTylkoQuizyClient" \
  -d "username=user1" \
  -d "password=1234" | jq -r '.access_token')

  curl -s -X GET "http://localhost:5002/api/users/" \
  -H "Authorization: Bearer $USER1_TOKEN" | jq

  curl -s -w "\nHTTP:%{http_code}" \
  -X POST "http://localhost:5002/api/users/logout" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json"


curl -X POST http://localhost:5001/quizzes/ \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Quiz bez tokenu",
    "category_id": 2,
    "questions": [
      {
        "question_text": "Pytanie testowe?",
        "options": [
          {"option_text": "A", "is_correct": true},
          {"option_text": "B", "is_correct": false}
        ]
      }
    ]
  }'